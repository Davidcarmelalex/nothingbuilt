# Contributing to NothingBuilt

This platform exists to give power back to artists. Every contribution should serve that mission.

## Setup
```bash
npm install && cp .env.example .env.local && npm run dev
```

## Commit Convention
```
feat(profile): add music player component
feat(rights): add on-chain ownership registration
fix: resolve audio upload chunking issue
test: add artist profile unit tests
```
